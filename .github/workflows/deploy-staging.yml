# Workflow de deploiement de l'environnement de RECETTE (staging).
#
# Independant de deploy.yml (production) : ce workflow ne se declenche
# JAMAIS automatiquement, uniquement a la demande (workflow_dispatch).
#
# Pour deployer une branche sur la VM de recette :
#   GitHub -> Actions -> "Deploy Staging" -> "Run workflow"
#   -> choisir la branche a valider -> Run workflow
#
# Le build part de la ref selectionnee dans l'UI (comportement par defaut
# de actions/checkout en workflow_dispatch). Voir docs/staging.md.
#
# Pourquoi ce workflow COMPILE, alors que deploy.yml (production) se contente de promouvoir
# l'artefact deja construit et teste par la CI : celle-ci n'empaquette un artefact que sur un
# tag `v*`, or la recette deploie une branche en cours de validation, pour laquelle il n'existe
# rien a promouvoir. La propriete visee par cette separation est neanmoins conservee — le job
# `build` n'a pas de cle `environment:`, il n'a donc acces a aucun secret de recette ; seul le
# job `deploy` en dispose. Aucune compilation n'a lieu en contexte privilegie.
#
# Ce build ne lance VOLONTAIREMENT ni typecheck, ni lint, ni tests : la recette sert a valider
# un travail en cours, y compris avant que la CI ne soit verte. On peut donc y deployer du code
# aux tests rouges — c'est assume ici, et impossible en production. Voir docs/staging.md.

name: Deploy Staging

on:
  workflow_dispatch:
    inputs:
      note:
        description: "Note libre (raison du deploiement, ex: validation feat/X)"
        required: false
        type: string

permissions:
  contents: read
  actions: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      NEXT_TELEMETRY_DISABLED: "1"
      DATABASE_URL: "mysql://dummy:dummy@localhost/dummy"
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: "22"
          cache: "npm"

      - name: Determine version
        id: version
        run: |
          PKG=$(node -p "require('./package.json').version")
          SHORT_SHA=$(git rev-parse --short HEAD)
          echo "version=${PKG}-${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - run: npm ci
      - run: npx prisma generate
      # `npm run build` produit aussi dist/worker.mjs (esbuild) — le worker audio est bundlé,
      # il n'a donc besoin ni de src/ ni de tsx sur le serveur (ADR-0007).
      #
      # NEXT_PUBLIC_BUILD_VERSION est inliné dans le bundle au moment du build : la version
      # affichée dans le footer devient celle du code réellement déployé (`1.18.0-abc1234`)
      # et non le `package.json` de la branche, figé sur la dernière version publiée. Le
      # transmettre au serveur ne suffirait pas — l'artefact doit le porter.
      #
      # Idem pour NEXT_PUBLIC_TURNSTILE_SITE_KEY (spec 030) : c'est une clé publique inlinée
      # côté client, la renseigner uniquement dans shared/.env sur le serveur de recette ne
      # suffit pas. Variable de repository (non secrète) donc lisible sans `environment:`, ce
      # qui préserve la séparation voulue par ce job (aucun secret privilégié en contexte
      # de compilation).
      - run: npm run build
        env:
          NEXT_PUBLIC_BUILD_VERSION: ${{ steps.version.outputs.version }}
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: ${{ vars.NEXT_PUBLIC_TURNSTILE_SITE_KEY }}

      # Élague les devDependencies APRÈS le build : l'archive n'embarque plus typescript,
      # vitest, eslint, esbuild… La CLI `prisma` est en `dependencies` (requise sur le serveur
      # pour `migrate deploy`) et survit donc à l'élagage.
      - name: Prune dev dependencies
        run: npm prune --omit=dev

      - name: Package artifact
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          tar czf "koinonia-${VERSION}.tar.gz" \
            --exclude='prisma/scripts' \
            .next/standalone .next/static dist \
            prisma prisma.config.ts public package.json \
            node_modules
          echo "Built koinonia-${VERSION}.tar.gz"
          du -sh "koinonia-${VERSION}.tar.gz"

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: koinonia-${{ steps.version.outputs.version }}
          path: koinonia-${{ steps.version.outputs.version }}.tar.gz
          retention-days: 14

    outputs:
      version: ${{ steps.version.outputs.version }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 10
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: koinonia-${{ needs.build.outputs.version }}

      - name: Upload artifact to server
        uses: appleboy/scp-action@917f8b81dfc1ccd331fef9e2d61bdc6c8be94634 # v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: koinonia-${{ needs.build.outputs.version }}.tar.gz
          target: /tmp

      - name: Deploy artifact via SSH
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        env:
          VERSION: ${{ needs.build.outputs.version }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          envs: VERSION,DEPLOY_PATH
          script: |
            set -e
            DEPLOY_PATH="${DEPLOY_PATH:-/opt/koinonia}"

            echo "Deploying staging v${VERSION}..."

            # 1. Prepare release directory
            mkdir -p "$DEPLOY_PATH/releases/koinonia-${VERSION}"
            cd "$DEPLOY_PATH/releases/koinonia-${VERSION}"

            # 2. Extract pre-built artifact
            tar xzf "/tmp/koinonia-${VERSION}.tar.gz"
            rm "/tmp/koinonia-${VERSION}.tar.gz"

            # 3. Assemble standalone bundle (Next.js standalone output)
            cp -r .next/static .next/standalone/.next/static
            cp -r public .next/standalone/public
            ln -sf "$DEPLOY_PATH/shared/.env" .next/standalone/.env

            # 4. Run migrations
            set -a; source "$DEPLOY_PATH/shared/.env"; set +a
            ./node_modules/.bin/prisma migrate deploy --schema=prisma/schema.prisma

            # 5. Activate release
            ln -sfn "$DEPLOY_PATH/releases/koinonia-${VERSION}" "$DEPLOY_PATH/current"

            # 6. Restart
            sudo systemctl restart koinonia

            # 6b. Restart audio worker (separate unit, ADR-0007) if installed on this host —
            # sans cela il continuerait de tourner sur le bundle de la release precedente.
            if systemctl list-unit-files | grep -q '^koinonia-audio-worker\.service'; then
              sudo systemctl restart koinonia-audio-worker
            else
              echo "koinonia-audio-worker.service absent — worker audio non deploye sur cet hote"
            fi

            # 7. Cleanup: keep last 3 releases
            cd "$DEPLOY_PATH/releases"
            ls -1dt koinonia-*/ | tail -n +4 | xargs rm -rf || true

            echo "Deploy staging v${VERSION} OK"
